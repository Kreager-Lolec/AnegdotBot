import random

import psycopg2
import Constant_File

mydb = psycopg2.connect(Constant_File.DB_URI, sslmode='require')


def CreateTable():
    mycursor = mydb.cursor()
    mycursor.execute('''
        CREATE TABLE IF NOT EXISTS CHATS (
            ChatId BIGINT PRIMARY KEY,
            CountUsers BIGINT ); ''')
    mydb.commit()
    mycursor.execute(''' 
        CREATE TABLE IF NOT EXISTS CATEGORIES (
            CaterogyId INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
            CategoryName VARCHAR(255) NOT NULL );
                   ''')
    mydb.commit()
    mycursor.execute('''
            CREATE TABLE IF NOT EXISTS ANEGDOTS (
                AnegdotId INTEGER NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                Anegdot VARCHAR(255) NOT NULL,
                CategoryName VARCHAR(255) NOT NULL
        );
                       ''')
    # Insert INTO CREATE TABLE IF YOU NEED THIS
    # CaterogyId INTEGER,
    # CONSTRAINT FK_AnegdotCatehogy1 FOREIGN KEY (CaterogyId) REFERENCES CATEGORIES(CaterogyId)
    mydb.commit()
    mycursor.close()


def checkIfExistChats():
    mycursor = mydb.cursor()
    mycursor.execute(f'''SELECT COUNT(*) FROM CHATS''')
    if mycursor.fetchone()[0] > 0:
        return True
    return False


def GetChatsId():
    mycursor = mydb.cursor()
    mycursor.execute(f'''SELECT * FROM CHATS''')
    listId = []
    for row in mycursor.fetchall():
        listId.append(row[0])
        print(row[0])
    return listId


def checkIfExistsCategory(message):
    mycursor = mydb.cursor()
    mycursor.execute(f'''SELECT COUNT(*) FROM CATEGORIES WHERE CategoryName = N'{message}' ''')
    if mycursor.fetchone()[0] == 1:
        return True
    return False


def checkIfExistsAnedgot(category, anegdot):
    mycursor = mydb.cursor()
    mycursor.execute(f'''SELECT COUNT(*) FROM ANEGDOTS WHERE CategoryName = N'{category}' AND Anegdot = N'{anegdot}' ''')
    if mycursor.fetchone()[0] == 1:
        return True
    return False


def checkIfNotExistAnedgots():
    mycursor = mydb.cursor()
    mycursor.execute(f'''SELECT COUNT(*) FROM ANEGDOTS''')
    if mycursor.fetchone()[0] == 0:
        return True
    return False


def checkIfNotExistAnedgotsByCategory(category):
    mycursor = mydb.cursor()
    mycursor.execute(f'''SELECT COUNT(*) FROM ANEGDOTS WHERE CategoryName = N'{category}' ''')
    if mycursor.fetchone()[0] == 0:
        return True
    return False


def addCategory(message):
    mycursor = mydb.cursor()
    mycursor.execute(f'''INSERT INTO CATEGORIES (CategoryName)
    SELECT N'{str(message.text)}'
    WHERE
        NOT EXISTS (SELECT * FROM CATEGORIES WHERE CategoryName = N'{str(message.text)}')''')
    mydb.commit()
    mycursor.close()


def addAnegdotToDb(message, category):
    mycursor = mydb.cursor()
    mycursor.execute(f'''INSERT INTO ANEGDOTS (Anegdot,CategoryName)
    SELECT N'{str(message.text)}', N'{category}'
    WHERE
        NOT EXISTS (SELECT * FROM ANEGDOTS WHERE CategoryName = N'{category}' AND Anegdot = N'{str(message.text)}')''')
    mydb.commit()
    mycursor.close()


def AddChat(message):
    mycursor = mydb.cursor()
    mycursor.execute(f'''INSERT INTO CHATS (ChatId)
    SELECT {message.chat.id}
    WHERE
        NOT EXISTS (SELECT * FROM CHATS WHERE ChatId = {message.chat.id} ) ''')
    mydb.commit()
    mycursor.close()


def getCategories():
    mycursor = mydb.cursor()
    mycursor.execute('''SELECT * FROM CATEGORIES''')
    info = mycursor.fetchall()
    listCategories = []
    for row in info:
        listCategories.append(row[1])
    print(listCategories)
    return listCategories


def getAnegdot():
    mycursor = mydb.cursor()
    mycursor.execute('''SELECT * FROM ANEGDOTS''')
    info = mycursor.fetchall()
    listAnegdots = []
    for row in info:
        listAnegdots.append(row[1])
    print(listAnegdots)
    anegdot = random.choice(listAnegdots)
    return anegdot


def getAnegdotByCategory(categoryname):
    mycursor = mydb.cursor()
    mycursor.execute(f'''SELECT * FROM ANEGDOTS WHERE CategoryName = N'{categoryname}' ''')
    info = mycursor.fetchall()
    listAnegdots = []
    for row in info:
        listAnegdots.append(row[1])
    print(listAnegdots)
    anegdot = random.choice(listAnegdots)
    return anegdot


def deleteCategory(category):
    print(category)
    mycursor = mydb.cursor()
    mycursor.execute(f"DELETE FROM CATEGORIES WHERE CategoryName = N'{category}' ")
    mydb.commit()
    mycursor.close()


def deleteAnegdotsByCategory(category):
    print(category)
    mycursor = mydb.cursor()
    mycursor.execute(f"DELETE FROM ANEGDOTS WHERE CategoryName = N'{category}' ")
    mydb.commit()
    mycursor.close()